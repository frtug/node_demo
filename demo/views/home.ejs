<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page%></title>
    <link rel="stylesheet" href="/css/home.css" />
</head>
<body>
    <%- include('./includes/navigation.ejs') %>

    <main class="content">
        <div class="movie-grid">
            <% for(let i=0; i<movies.length; i++) { %>
                <div class="movie-card">
                    <h2 class="movie-title"><%=movies[i].title%> </h2>
                    <div class="movie-poster">
                        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcR9SRRmhH4X5N2e4QalcoxVbzYsD44C-sQv-w&s" alt="<%=movies[i].title%> poster">
                    </div>
                    <div class="movie-details">
                        <span class="rating">Rating: <%= movies[i].rating %></span>
                        <span class="genre">Genre: <%= movies[i].tags %></span>
                    </div>
                    <p class="movie-description"><%= movies[i].desc %></p>
                    <div class="movie-actions">
                        <form class="movie-action-form delete-form" action="/admin/delete/<%= movies[i]._id %>" method="POST">
                            <button class="action-button delete-button" type="submit">Delete</button>
                        </form>
                        <form class="movie-action-form details-form" action="/admin/edit-details/<%= movies[i]._id %>" method="POST">
                            <button class="action-button details-button" type="submit">Edit Details</button>
                        </form>
                    </div>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="main-footer">
        <p class="copyright">© <%= new Date().getFullYear() %> Movie Database</p>
    </footer>
</body>
</html>